{
  "name": "nio-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "console",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n backend,console -c cyan,magenta \"npm run dev:backend\" \"npm run dev:console\"",
    "dev:backend": "npm run start --workspace=backend",
    "dev:console": "npm run dev --workspace=console",
    "build": "npm run build --workspace=backend && npm run build --workspace=console",
    "typecheck": "npm run typecheck --workspace=backend && npm run typecheck --workspace=console",
    "db:up": "docker compose up -d postgres",
    "db:migrate": "npm run db:push --workspace=console"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
